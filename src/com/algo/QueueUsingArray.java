package com.algo;

//Queue-- FIFO -- means the element which is inserted first and same the same element will get out(removed)
/*
 * Array implementation of Queue is not dynamic in nature.
There are two most important operations of Queue:
enQueue: It is operation when we insert element into the queue.
deQueue: It is operation when we remove element from the queue.
Adding data at rear side. (bottom)(backside)
Remove data from front side.(top)(frontside)
 * 
 */
public class QueueUsingArray 
{
	int capacity;
	int arr[];
	int front;
	int rear;
	int currentsize;

}
